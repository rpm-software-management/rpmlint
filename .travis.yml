language: python

python:
  - "2.6"
  - "2.7"

install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq
      python-rpm python-magic python-enchant
  - pip install pytest

script:
  - make check
