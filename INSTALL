You need the following utilities to run rpmlint:

	o python 2.3 or newer
	o python rpm support
	o find and xargs
	o readelf
	o file
	o cpio

and the following programs to install it:

	o python 2.3 or newer
	o sed
